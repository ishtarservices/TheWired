# Client build-time variables (Vite inlines these during `vite build`)
VITE_RELAY_URL=wss://relay.thewired.app
VITE_API_URL=https://api.thewired.app/api
# Optional: generate with `npx web-push generate-vapid-keys` (enables browser push notifications)
VITE_VAPID_PUBLIC_KEY=
